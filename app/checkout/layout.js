export const metadata = {
  title: 'Checkout',
  description: 'Finaliza tu pedido en Velour Studio.',
  robots: { index: false, follow: false },
};

export default function CheckoutLayout({ children }) {
  return children;
}
